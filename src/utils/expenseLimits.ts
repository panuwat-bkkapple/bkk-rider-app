// เพดาน/เส้นตายของการเบิกค่าใช้จ่าย — **สำเนาสำหรับ "บอกใบ้" บนฟอร์มเท่านั้น**
//
// ตัวบังคับจริงอยู่ฝั่ง server ที่ `functions/src/riderExpensePolicy.ts`
// (ไรเดอร์ยิง callable ตรงได้ ฟอร์มจึงกันอะไรไม่ได้เลย) แต่ฟอร์มต้องรู้ตัวเลข
// เพื่อ **บอกก่อนที่เขาจะเสียเวลา** — โดยเฉพาะตอนออฟไลน์ ซึ่งถ้าไม่บอกตอนนั้น
// เขาจะรู้ว่าส่งไม่ได้ก็ตอนขึ้นมาชั้นบนแล้วคิว flush ไม่ผ่าน
//
// **MIRROR — แก้ที่นี่ต้องแก้ที่ `functions/src/riderExpensePolicy.ts` ด้วย**
// (import ข้ามไม่ได้: functions มี rootDir ของตัวเอง) ด่านคือ
// `expenseLimitsParity.test.ts` ซึ่งเทียบกับข้อความจริงในไฟล์นั้น
//
// **ค่าที่แอดมินตั้งไว้ใน `settings/rider_expense` ทับค่าพวกนี้ฝั่ง server**
// ไฟล์นี้จึงเป็นแค่ค่าเริ่มต้นสำหรับข้อความบนจอ ไม่ใช่ความจริงสุดท้าย —
// ถ้าแอดมินขยับเพดาน ฟอร์มจะเตือนตามค่าเก่าแต่ server ยังตัดสินถูก
// ซึ่งเป็นทิศที่ยอมรับได้ (เตือนเกิน ไม่ใช่ปล่อยผ่าน)

export const RIDER_EXPENSE_DEFAULTS = {
  manager_max_per_item: 500,
  hard_max_per_item: 2000,
  ceo_threshold_per_job: 1000,
  normal_backdate_days: 7,
  hard_backdate_days: 31,
} as const;
